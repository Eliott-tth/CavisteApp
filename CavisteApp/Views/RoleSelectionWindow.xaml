<Window x:Class="CavisteApp.Views.RoleSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Caviste - Connexion" Height="340" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Connexion" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Text="Choisis ton rôle :" Margin="0,0,0,8"/>

        <RadioButton x:Name="RadioVisiteur" Grid.Row="2" Content="Visiteur (consultation uniquement)"
                     IsChecked="True" GroupName="Role" Margin="0,0,0,8"
                     Checked="RadioRole_Checked"/>
        <RadioButton x:Name="RadioAdministrateur" Grid.Row="3" Content="Administrateur (accès complet)"
                     GroupName="Role" Margin="0,0,0,8"
                     Checked="RadioRole_Checked"/>

        <StackPanel Grid.Row="4" Margin="0,8,0,16" x:Name="PanelMotDePasse" Visibility="Collapsed">
            <TextBlock Text="Mot de passe administrateur :"/>
            <PasswordBox x:Name="ChampMotDePasse" Margin="0,4,0,4"/>
            <TextBlock x:Name="TexteErreur" Foreground="Crimson" TextWrapping="Wrap"/>
        </StackPanel>

        <Button Grid.Row="5" Content="Entrer" Padding="14,8" HorizontalAlignment="Right"
                VerticalAlignment="Bottom" Click="BoutonEntrer_Click"/>
    </Grid>
</Window>
