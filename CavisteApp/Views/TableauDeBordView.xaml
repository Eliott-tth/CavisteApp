<UserControl x:Class="CavisteApp.Views.TableauDeBordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:CavisteApp.ViewModels">

    <UserControl.DataContext>
        <vm:TableauDeBordViewModel/>
    </UserControl.DataContext>

    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Tableau de bord" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Rafraîchir" Command="{Binding ChargerCommand}" Margin="16,0,0,0" Padding="10,4"/>
        </StackPanel>

        <UniformGrid Rows="1" Columns="4" Margin="0,0,0,20">
            <Border Background="White" BorderBrush="#E0D5D2" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Vins en stock" Foreground="DimGray"/>
                    <TextBlock Text="{Binding NombreVins}" FontSize="28" FontWeight="Bold" Foreground="#7A1F2B"/>
                    <TextBlock Text="références" Foreground="DimGray" FontSize="11"/>
                </StackPanel>
            </Border>
            <Border Background="White" BorderBrush="#E0D5D2" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Stock total" Foreground="DimGray"/>
                    <TextBlock Text="{Binding StockTotal}" FontSize="28" FontWeight="Bold" Foreground="#7A1F2B"/>
                    <TextBlock Text="bouteilles" Foreground="DimGray" FontSize="11"/>
                </StackPanel>
            </Border>
            <Border Background="White" BorderBrush="#E0D5D2" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Ventes du mois" Foreground="DimGray"/>
                    <TextBlock Text="{Binding VentesDuMois}" FontSize="28" FontWeight="Bold" Foreground="#7A1F2B"/>
                    <TextBlock Text="transactions" Foreground="DimGray" FontSize="11"/>
                </StackPanel>
            </Border>
            <Border Background="White" BorderBrush="#E0D5D2" BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Alertes stock bas" Foreground="DimGray"/>
                    <TextBlock Text="{Binding NombreAlertes}" FontSize="28" FontWeight="Bold" Foreground="Crimson"/>
                    <TextBlock Text="vins" Foreground="DimGray" FontSize="11"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" BorderBrush="#E0D5D2" BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Alertes de stock bas" FontWeight="Bold" FontSize="15" Margin="0,0,0,10"/>
                    <ItemsControl ItemsSource="{Binding VinsEnAlerte}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Nom}"/>
                                    <TextBlock Grid.Column="1" Foreground="Crimson"
                                               Text="{Binding Stock, StringFormat='Stock : {0}'}"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" BorderBrush="#E0D5D2" BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Ventes récentes" FontWeight="Bold" FontSize="15" Margin="0,0,0,10"/>
                    <ItemsControl ItemsSource="{Binding VentesRecentes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Client.NomComplet}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding MontantTotal, StringFormat={}{0:N2} €}"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>
    </StackPanel>
</UserControl>
