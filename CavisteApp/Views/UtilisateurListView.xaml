<UserControl x:Class="CavisteApp.Views.UtilisateurListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:CavisteApp.ViewModels">

    <UserControl.DataContext>
        <vm:UtilisateurListViewModel/>
    </UserControl.DataContext>

    <DockPanel Margin="0">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Utilisateurs" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Rafraîchir" Command="{Binding ChargerCommand}" Margin="16,0,0,0" Padding="10,4"/>
        </StackPanel>

        <TextBlock DockPanel.Dock="Bottom" Text="{Binding MessageStatut}" Margin="0,8,0,0" Foreground="DimGray"/>

        <DataGrid ItemsSource="{Binding Utilisateurs}" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Rôle" Binding="{Binding Role}" Width="130"/>
                <DataGridTextColumn Header="Confirmé" Binding="{Binding EstConfirme}" Width="90"/>
                <DataGridTextColumn Header="Créé le" Binding="{Binding DateCreation, StringFormat={}{0:dd/MM/yyyy}}" Width="110"/>
                <DataGridTemplateColumn Header="Actions" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Changer le rôle"
                                        Command="{Binding DataContext.BasculerRoleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" Padding="8,3" Margin="0,2,6,2"/>
                                <Button Content="Supprimer"
                                        Command="{Binding DataContext.SupprimerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" Padding="8,3" Margin="0,2,0,2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
